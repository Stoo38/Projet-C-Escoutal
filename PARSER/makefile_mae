CC = g++			#Compilater used
EXEC_NAME = BIN/main		#Name of the executable

all : $(EXEC_NAME)

$(EXEC_NAME) : create_object_files
	$(CC) -o $(EXEC_NAME) main.o

create_object_files :  
	$(CC) -c -o main.o main.cpp

clean :
	rm -f $(EXEC_NAME)
	rm -f main.o

distclean :
